apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer  # Utilisez "ClusterIP" si vous ne prévoyez pas d'exposer le service en dehors du cluster


